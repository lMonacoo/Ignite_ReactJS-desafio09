module.exports = {
  // usamos esse next/babel porque estamos em um projeto que utiliza next
  // se não estivessemos em um projeto com next provavelmente utilizariamos -> babel-preset-env
  presets: ['next/babel']
};
